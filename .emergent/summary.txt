You are working on a Universal AI Dashboard, a 100% client-side React application for in-browser machine learning.

The application is currently broken because /app/frontend/src/App.js has corrupted JSX due to multiple failed search_replace operations.

Your task is to fix the application and complete the Predictions Workspace refactor in a single pass, while minimizing unnecessary edits and avoiding repeated agent runs.

PRIMARY OBJECTIVE

Repair the corrupted JSX in:

/app/frontend/src/App.js

and implement a Unified Predictions Workspace.

Do NOT perform incremental edits.
Do NOT attempt multiple small replacements.

Instead:

• Identify the corrupted JSX section
• Replace it with a clean implementation
• Ensure the application compiles successfully

REQUIRED UI ARCHITECTURE

The dashboard sidebar should contain:

• Dashboard
• Dataset Scanner
• Analysis
• Predictions

The Clusters tab must be removed completely.

Clusters are not a separate page.

PREDICTIONS WORKSPACE BEHAVIOR

The Predictions page must dynamically adapt based on the dataset type.

If the dataset is SUPERVISED

Show:

• Model predictions
• Actual vs Predicted scatter plot
• Model performance metrics
• Model comparison chart
• Feature importance chart
• Residual plot

Charts must be implemented using Recharts with proper:

• titles
• legends
• axis labels

If the dataset is UNSUPERVISED

Show clustering results inside Predictions.

Display:

• clustering algorithm leaderboard
• PCA cluster visualization
• t-SNE cluster visualization
• cluster distribution chart

Clustering algorithms:

• K-Means
• Hierarchical Clustering
• DBSCAN
• Gaussian Mixture Model (GMM)

Evaluation metrics:

• Silhouette Score
• Davies-Bouldin Index
• Calinski-Harabasz Score

CORRELATION ANALYSIS TOOL

Inside Predictions add a Correlation Explorer.

Users must be able to:

• select any two variables
• generate a scatter plot
• view Pearson correlation value

Also generate a correlation heatmap for numeric features.

Use Recharts for scatter plots.

UNSUPERVISED ML ENGINE

Unsupervised logic must live in:

/app/frontend/src/unsupervisedML.js

Replace placeholder functions with real implementations.

Include:

Clustering:

• K-Means
• Hierarchical Clustering
• DBSCAN
• GMM

Dimensionality reduction:

• PCA
• t-SNE

Anomaly detection:

• Isolation Forest
• Local Outlier Factor

All algorithms must run fully client-side using JavaScript.

EXPLAINABILITY REQUIREMENT

All ML metrics must include plain-English explanations.

Examples:

Silhouette Score:

“Measures how well points fit within their cluster. Higher values mean clusters are clearly separated.”

F1 Score:

“Balances precision and recall. Higher values mean the model makes fewer mistakes.”

Use tooltips or small explanation cards.

PERFORMANCE RULES (IMPORTANT)

To minimize credit usage and processing cost:

• Do not refactor the entire project
• Do not split App.js into new components
• Do not modify unrelated files
• Only edit the necessary JSX sections

Focus only on:

App.js
unsupervisedML.js
SUCCESS CONDITIONS

The task is complete when:

• The application compiles successfully
• The Predictions workspace renders correctly
• The Clusters tab is removed
• Supervised datasets show predictions
• Unsupervised datasets show clustering results
• Correlation analysis tool works

IMPORTANT

Avoid repeated search_replace operations.

Perform one clean JSX replacement for the corrupted section.

Ensure the UI renders without duplicate sections or orphaned tags.

Expected Result

A working Universal AI Dashboard where:

Predictions becomes the central ML workspace showing:

• supervised model predictions
• unsupervised clustering results
• correlation analysis tools

All running entirely in the browser.
